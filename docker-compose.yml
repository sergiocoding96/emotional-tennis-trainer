services:
  postgres:
    image: postgres:16-alpine
    container_name: tennisaffect-arousal-db
    environment:
      POSTGRES_USER: tennisaffect
      POSTGRES_PASSWORD: ${DB_PASSWORD:-devpassword123}
      POSTGRES_DB: tennisaffect_arousal
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tennisaffect"]
      interval: 5s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: tennisaffect-arousal-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@tennisaffect.local
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
    ports:
      - "5052:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
